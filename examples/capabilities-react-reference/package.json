{
  "name": "@engineering-ui-kit/example-react-reference",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "React-web + Electron reference slices proving the Capabilities TS runtime's ./react hook and ./electron IPC adapters work end-to-end through a real composition root: a single-deployable React UI triggering an operation via useOperation, and an Electron renderer -> main IPC round-trip reaching an operation through the typed IPC boundary.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@engineering-ui-kit/capabilities-runtime": "*"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "electron": "^43.0.0",
    "jsdom": "^25.0.1",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
